{
  "name": "acumen-logic-model-2-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for Render — installs server deps and starts the app",
  "scripts": {
    "build": "cd server && npm install",
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
